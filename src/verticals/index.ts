/**
 * Vertical registration.
 *
 * **This is the file a new vertical edits — the only one outside its own
 * package.** Import the vertical's manifest here and it appears in the launcher,
 * the super-admin entitlement UI, and anything else that lists verticals.
 *
 * Imported once from `main.tsx` for its side effect.
 */
export { GRANT } from "./grant/manifest"
export { HONOR } from "./honor/manifest"

// ── New verticals go here ────────────────────────────────────────
// export { ACME } from "./acme/manifest"
