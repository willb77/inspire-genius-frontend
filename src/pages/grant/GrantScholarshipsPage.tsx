import { Award } from "lucide-react"
import GrantStubPage from "./GrantStubPage"

export default function GrantScholarshipsPage() {
  return (
    <GrantStubPage
      title="Scholarships"
      description="Matched scholarships ranked by relevance to your profile."
      icon={Award}
    />
  )
}
