/**
 * Honor media assets. Videos are hosted on the shared public-videos bucket
 * (referenced by absolute URL, so one upload serves every environment) — the
 * same pattern the Home surface uses. Not bundled with the app.
 */

/** PRISM overview explainer (h264 1080p, ~5:49). */
export const PRISM_OVERVIEW_VIDEO_URL =
  "https://ig-demo-public-videos.s3.amazonaws.com/PRISM_Overview.mp4"
